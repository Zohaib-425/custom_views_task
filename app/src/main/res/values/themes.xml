<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CustomViewsTask" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">@color/colorPrimary
        </item>
    </style>

    <style name="Theme.CustomViewsTask" parent="Base.Theme.CustomViewsTask">
        <item name="fontFamily">@font/poppins</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
    </style>
</resources>